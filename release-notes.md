粉笔题目收进思源笔记 v0.2.0

每次收录为思源笔记新建一篇文档，文档名按「科目·模块·日期·短哈希」生成；
答案与解析作为独立的思源 HTML 块插入，默认折叠，并位于选项之后。

## 主要功能
- 右下角按钮收进思源：按钮位于每个题目卡片右下角，按题即点即收。
- 一题一文：每道题在指定笔记本/父路径下新建独立文档，便于后续按考点标签复习。
- 多级标签：引用块内写入 #科目/模块/考点# 形式，标签在思源中按层级归类。
- 选项解析排版：各选项解析按段拆分并加粗「A项正确 / B项错误」，不再挤成一段。
- 答案 / 解析默认折叠：文档中以思源原生 HTML 块（<details>）呈现，默认不展开。
- 科目与模块记忆：弹窗填写的科目、模块默认沿用上次的值，减少重复输入。

## 安装方式
1. 下载 fenbi-siyuan-0.2.0.zip 并解压。
2. Chrome / Edge 打开扩展程序页，开启「开发者模式」。
3. 点击「加载已解压的扩展程序」，选择解压后的目录。
4. 打开扩展选项，填入思源 API 地址、Token、笔记本 ID、父路径。
5. 在粉笔题目页点击「收进思源」。

注意：crx 文件因自签名无法直接拖放安装，请优先使用 zip 解压后「加载已解压的扩展程序」。
